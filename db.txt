📌 Database Schema — Study_Mate (PostgreSQL)
1. users

Holds core user identity + account metadata.

Column	Type	Constraints	Notes
id	UUID	PK	auto-generated
name	VARCHAR(100)	NOT NULL	
email	VARCHAR(150)	UNIQUE, NOT NULL	login
password_hash	TEXT	NOT NULL	secure storage
institution	VARCHAR(150)	NULL	optional
academic_level	VARCHAR(50)	NULL	optional (e.g. "Year 2", "Diploma")
created_at	TIMESTAMP	DEFAULT now()	
updated_at	TIMESTAMP	DEFAULT now()	
2. units

Stores each unit/course the user is studying.

Column	Type	Constraints
id	UUID	PK
user_id	UUID	FK → users(id)
name	VARCHAR(150)	NOT NULL
difficulty	VARCHAR(20)	NOT NULL (easy, moderate, hard)
type	VARCHAR(30)	NOT NULL (exam, practical, project)
expected_assessment_date	DATE	NULL
weight_score	INTEGER	computed or stored, optional
created_at	TIMESTAMP	DEFAULT now()
3. availability

Captures the user’s weekly and daily available hours.

Column	Type	Constraints
id	UUID	PK
user_id	UUID	FK → users(id)
day_of_week	VARCHAR(20)	NOT NULL (Mon–Sun)
available_hours	INTEGER	NOT NULL
preferred_session	VARCHAR(30)	NULL (morning/afternoon/evening)
4. study_plans

Each generated study plan is stored, allowing regeneration, comparison, or progress tracking.

Column	Type	Constraints
id	UUID	PK
user_id	UUID	FK → users(id)
generated_at	TIMESTAMP	DEFAULT now()
version	INTEGER	DEFAULT 1
is_active	BOOLEAN	DEFAULT TRUE
5. study_sessions

Stores the actual timetable blocks generated by the engine.

Column	Type	Constraints
id	UUID	PK
plan_id	UUID	FK → study_plans(id)
unit_id	UUID	FK → units(id)
session_date	DATE	NOT NULL
start_time	TIME	NOT NULL
end_time	TIME	NOT NULL
session_type	VARCHAR(30)	deep_focus / sprint / revision
status	VARCHAR(20)	default ‘pending’ (pending/completed/missed)
6. progress_logs

Logs user’s progress for adaptive rescheduling.

Column	Type	Constraints
id	UUID	PK
session_id	UUID	FK → study_sessions(id)
user_id	UUID	FK → users(id)
status	VARCHAR(20)	completed / missed
notes	TEXT	NULL
logged_at	TIMESTAMP	DEFAULT now()
7. stress_index (optional add-on)

Tracks user workload health over time.

Column	Type	Constraints
id	UUID	PK
user_id	UUID	FK → users(id)
score	INTEGER	NOT NULL (0–100)
computed_at	TIMESTAMP	DEFAULT now()
8. revision_assets (optional AI add-on)

Stores auto-generated flashcards, summaries, etc.

Column	Type	Constraints
id	UUID	PK
unit_id	UUID	FK → units(id)
asset_type	VARCHAR(30)	flashcard / summary / quiz
content	TEXT	NOT NULL
created_at	TIMESTAMP	DEFAULT now()
📌 ERD (Entity Relationships Summary)

users 1—N units

users 1—N availability

users 1—N study_plans

study_plans 1—N study_sessions

units 1—N study_sessions

study_sessions 1—N progress_logs

users 1—N progress_logs

users 1—N stress_index (optional)

units 1—N revision_assets (optional)